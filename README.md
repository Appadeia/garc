# garc

arc, but for gitlab.

## example configuration

```toml
[[remote]]
token = "API_TOKEN"
url = "invent.kde.org"
username = "cblack"
```
`~/.garcrc`

repeat this configuration for each gitlab instance you want
to use garc for.